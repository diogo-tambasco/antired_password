# Copie para .env (fora do git) e preencha. NÃO versione o .env real.
# RAILS_MASTER_KEY = conteúdo de config/master.key (gitignored).
RAILS_MASTER_KEY=cole_aqui_o_conteudo_de_config_master_key
# Domínio que aponta (registro A) pro IP da VPS — Caddy emite o cert pra ele.
ANTIRED_DOMAIN=seu-subdominio.duckdns.org
