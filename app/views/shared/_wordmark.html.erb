<%# Wordmark do app — ênfase no nome. local opcional: size_classes (ex. "text-2xl") %>
<span class="font-mono font-bold tracking-tight <%= local_assigns.fetch(:size_classes, "text-xl") %>"><span class="text-white">antired</span><span class="text-emerald-400">_password</span></span>
